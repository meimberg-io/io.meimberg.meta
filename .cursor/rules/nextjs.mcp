# nextjs.mcp
# Rules for Next.js 15 frontend projects with App Router
# Author: Oli Meimberg
# Version: 1.0

rules:
  - type: edit
    pattern: "app/**/*.{ts,tsx}"
    allow: true
    message: "Next.js App Router edits allowed"

  - type: edit
    pattern: "components/**/*.{ts,tsx}"
    allow: true
    message: "Component edits allowed"

  - type: style
    framework: "nextjs"
    version: "15"
    note: >
      Next.js 15 conventions:
      - Use App Router (app/ directory)
      - Prefer React Server Components by default
      - Add 'use client' only when necessary (interactivity, hooks, browser APIs)
      - Keep server and client components separate
      - Use Tailwind + DaisyUI for styling
      - No inline styles - use Tailwind classes
      - Functional components with hooks only
      - Props interfaces with TypeScript

  - type: custom
    note: >
      File structure:
      - app/ - routes and layouts (server components)
      - components/ - reusable UI components
      - lib/ - utilities and helpers
      - types/ - TypeScript type definitions
      - public/ - static assets

  - type: custom
    note: >
      Best practices:
      - Use async/await for data fetching in server components
      - Implement proper loading and error states
      - Use dynamic imports for code splitting
      - Optimize images with next/image
      - Use metadata API for SEO
      - Keep components small and focused

